# Backend API URL (for local development only)
# In production, Vercel proxies /api/* requests via vercel.json
VITE_API_URL=http://localhost:8000

# Note: Do NOT add VITE_API_URL in Vercel environment variables
# The vercel.json proxy handles production API routing
